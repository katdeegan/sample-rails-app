<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <!-- Rails looks for a partial _user.html.erb when "render user" is called -->
    <%= render user %>
  <% end %>
</ul>

<%= will_paginate %>